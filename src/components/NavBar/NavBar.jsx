import css from './NavBar.module.sass'

const NavBar = (props) => {
    return (<div className={css.NavBar}>
        <div>
            <span> </span>
            <span> </span>
            <span> </span>
        </div>

    </div>)
}
export default NavBar