

let initialState = {
    mathTopics:[
        {id:1,topic:'hellomath'},
        {id:2,topic:'hello2math'}
    ],
    phisicTopics:[
        {id:1,topic:'hellopis'},
        {id:2,topic:'hello2phis'}
    ]
}


const choosingPageReducer = (state = initialState, action) => {
    switch (action.type) {



        default:
            return state
    }
}


export default choosingPageReducer